import { HttpException } from '@nestjs/common';

// export const keyCheck = (body) => {
//   if (body.key !== process.env.KEY) {
//     throw new HttpException('Unable to access.', 403);
//   }
// };

// export const keyCheck2 = (body) => {
//   if (body.key !== process.env.KEY2) {
//     throw new HttpException('Unable to access.', 403);
//   }
// };
